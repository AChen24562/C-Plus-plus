//Name
// WK6 - PE

#include <iostream>
using namespace std;

int main() {
    int minVal, maxVal;
    cout << "Enter a min: ";
    cin >> minVal;
    cout << "Enter a max: ";
    cin >> maxVal;

    //Request values for minimum and maximum.
    //Output all numbers from minimum 	to maximum inclusive. (use it as a reference for HW6-1)
    for(int i = minVal; i <= maxVal; i++)
      cout << i << " ";






    return 0;
}
